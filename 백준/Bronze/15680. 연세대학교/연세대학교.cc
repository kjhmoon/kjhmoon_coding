#include <iostream>
#include <string>
#include <stdlib.h>
#include <cmath>
#include <algorithm>
#include <vector>
#include <iomanip>
#include <regex>
using namespace std;

int main() {
    int N;
    cin >> N;
    if (N == 1) {
        cout << "Leading the Way to the Future";
    }
    if (N == 0) {
        cout << "YONSEI";
    }
    return 0;
}